spring.application.name=invoice-analysis

# 模板数据源配置
# filesystem: 从JSON文件加载（开发调试用）
# database: 从MySQL数据库加载（生产环境用）
template.source=filesystem

# 文件系统模板路径（当template.source=filesystem时生效）
template.filesystem.path=classpath:templates/invoice
# 模板缓存开关（调试时建议关闭，修改模板后即时生效）
template.cache.enabled=false

# H2内存数据库配置（用于filesystem模式下避免JPA初始化错误）
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# MySQL数据库配置（当template.source=database时，替换上面的H2配置）
# spring.datasource.url=jdbc:mysql://localhost:3306/invoice_analysis?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
# spring.datasource.username=root
# spring.datasource.password=your_password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# 日志配置
logging.level.com.kinghy.invoiceanalysis=INFO
logging.level.com.kinghy.invoiceanalysis.strategy=DEBUG
